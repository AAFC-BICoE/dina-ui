{
  "name": "dina-ui",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "common-ui/react-hooks-async"
    ]
  },
  "scripts": {
    "test": "yarn workspaces run test",
    "test:coverage": "yarn workspaces run test:coverage"
  }
}
